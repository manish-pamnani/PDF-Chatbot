# Prereuisites

- Chroma DB
- LangChain
- Ollama (llama3.2:3b)
- Docker

# Installation

- Install the dependecies via:

```
npm install
```

- Start the Chroma Server on CLI using:

```
npx chroma run --path ./PDF-ChatBot
```

- Start the Script using:

```
node index.js
```